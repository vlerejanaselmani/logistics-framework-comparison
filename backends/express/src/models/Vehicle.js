export class Vehicle {
  constructor({ id, registrationNumber, type, capacityKg, available }) {
    this.id = id
    this.registrationNumber = registrationNumber
    this.type = type
    this.capacityKg = capacityKg
    this.available = available
  }
}
